/*wap to find the sum of numbers first and last digit*/
#include<stdio.h>
#include<conio.h>
void main()
{
	clrscr();
	int number,first,last,sum;

	printf("Enter any number:");
	scanf("%d",&number);

	last=number %10;
	while(number>=10)
	{
		number=number/10;
	}
	first=number;
	sum=first+last;
	printf("The sum of the first and last digit: %d",sum);
	getch();
}